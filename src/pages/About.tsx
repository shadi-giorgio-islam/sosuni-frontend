import Template from "../components/template/Template";
import React from "react";

export const About = () => {
    return (
        <Template>
            <h1>About</h1>
        </Template>
    );
}